{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA C++: Launch",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/matrix_mul",

            "environment": [
                {
                    "name": "LD_LIBRARY_PATH",
                    "value": "/root/miniconda3/lib:${env:LD_LIBRARY_PATH}"
                }
            ]
        },
    ]
}